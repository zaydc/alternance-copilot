# Copier ce fichier en .env puis remplir les valeurs (ne jamais committer .env)

# Jeton généré par `claude setup-token` (abonnement Pro) — lu par le Claude Agent SDK
CLAUDE_CODE_OAUTH_TOKEN=

# Jeton de l'API La bonne alternance (api.apprentissage.beta.gouv.fr)
LBA_API_TOKEN=
